import { ActiveHighlightDirective } from './active-highlight.directive';

describe('ActiveHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
